/**
 * @file secrets.h.example
 * @brief Template for secrets.h - SENSITIVE CREDENTIALS FILE
 *
 * ⚠️ WARNING: This file contains sensitive credentials!
 *
 * SETUP INSTRUCTIONS:
 * 1. Copy this file to secrets.h: cp secrets.h.example secrets.h
 * 2. Edit secrets.h with your actual credentials
 * 3. NEVER commit secrets.h to Git (already in .gitignore)
 *
 * @license GPL-3.0
 */

#pragma once

// WiFi Credentials
#define WIFI_SSID "Your_WiFi_Name"
#define WIFI_PASSWORD "Your_WiFi_Password"

// OTA Password
#define OTA_PASSWORD "openlux123"

// Network Configuration (optional - uses DHCP if commented out)
#define USE_STATIC_IP
#ifdef USE_STATIC_IP
#define STATIC_IP IPAddress(192, 168, 1, 58)
#define GATEWAY IPAddress(192, 168, 1, 1)
#define SUBNET IPAddress(255, 255, 255, 0)
#define DNS1 IPAddress(192, 168, 1, 11)
#endif
